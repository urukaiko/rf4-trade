{
  "name": "rf4-trade",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "dev:db": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db",
    "dev:full": "bun run dev:db && bun run dev",

    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",

    "test": "DATABASE_URL=\"${DATABASE_URL_TEST:-postgresql://rf4user:rf4pass@localhost:5432/rf4trade_test}\" vitest run",
    "test:watch": "DATABASE_URL=\"${DATABASE_URL_TEST:-postgresql://rf4user:rf4pass@localhost:5432/rf4trade_test}\" vitest",
    "test:e2e": "playwright test",

    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun src/lib/server/db/seed.ts",
    "db:up": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db",
    "db:down": "docker compose -f docker-compose.yml -f docker-compose.dev.yml down",
    "db:logs": "docker compose -f docker-compose.yml -f docker-compose.dev.yml logs -f db",
    "db:reset": "docker compose -f docker-compose.yml -f docker-compose.dev.yml down -v"
  },
  "dependencies": {
    "better-auth": "^1.6.1",
    "drizzle-orm": "^0.44.0",
    "lucide-svelte": "^1.0.1",
    "pg": "^8.20.0",
    "postgres": "^3.4.0",
    "svelte-5-french-toast": "^2.0.6",
    "valibot": "^1.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@sveltejs/kit": "^2.16.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/svelte": "^5.3.1",
    "@testing-library/user-event": "^14.6.1",
    "@types/pg": "^8.20.0",
    "@unocss/svelte-scoped": "^66.0.0",
    "drizzle-kit": "^0.30.0",
    "happy-dom": "^20.8.9",
    "svelte": "^5.0.0",
    "svelte-adapter-bun": "^1.0.1",
    "svelte-check": "^4.0.0",
    "typescript": "^5.7.0",
    "unocss": "^66.0.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.4"
  }
}